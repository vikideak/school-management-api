services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: schooldb
      POSTGRES_USER: apiuser
      POSTGRES_PASSWORD: complicatedpassword
    ports:
      - "5432:5432"

  api:
    build: .
    depends_on:
      - db
    ports:
      - "8080:8080"